{
  "name": "base-flashloan-arb-bot",
  "version": "0.1.0",
  "type": "module",
  "description": "Flashloan arbitrage bot for Base mainnet using Aave V3 and private bundles",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "smoke": "node scripts/smoke.js",
    "lint": "echo \"No linter configured\"",
    "deploy": "node scripts/deploy.js",
    "compile": "npx hardhat compile",
    "test": "echo \"No tests configured\""
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.17.0",
    "decimal.js": "^10.4.3",
    "express": "^4.18.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.13",
    "hardhat": "^3.0.15"
  }
}
